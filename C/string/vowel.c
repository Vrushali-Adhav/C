#include<stdio.h>
int main()
{
	char ch,a,A,e,E,I,i,O,o,U,u,S,s;
	printf("enter any character =");
	scanf("%c",&ch);
	if(ch==a||ch==A)
	{
		printf("%c is vowel",a);
		
	}
	else if(ch==e||ch==E)
	{
		printf("%c is vowel",ch);
	}
	else if(ch==i||I)
	{
				printf("%c is vowel",ch);

	
	}
	else if(ch==o||ch==O)
	{
				printf("%c is vowel",ch);

	}
	else if(ch==u||ch==U)
	{
				printf("%c is vowel",ch);

	}
	else if(ch==s||ch==S)
	{
				printf("%c is vowel",ch);

	}
	else
	{
		printf("%c is not vowel",ch);
	}
	
	
}
